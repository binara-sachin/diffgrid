{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the M4 settings window",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
