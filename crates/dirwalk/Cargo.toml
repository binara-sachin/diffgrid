[package]
name = "dirwalk"
version = "0.1.0"
edition.workspace = true

[dependencies]
ignore = "0.4"
serde = { version = "1", features = ["derive"] }
unicode-normalization = "0.1"

[dev-dependencies]
serde_json = "1"
criterion = "0.5"

[[bench]]
name = "scan"
harness = false
