[package]
name = "diff-core"
version = "0.1.0"
edition.workspace = true

[dependencies]
imara-diff = "0.2"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
criterion = "0.5"

[[bench]]
name = "diff_lines"
harness = false
